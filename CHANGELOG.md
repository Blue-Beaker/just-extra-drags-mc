## 1.0.1
Added patch to JustEnoughDrags to prevent crashing on dedicated server  
## 1.1.0
Added dumping clicked slots for debug mode  
Added slot index parameter for custom slots  
Removed dependency of JustEnoughDrags (the patch is still inluded!)  
Reworked network handler to prevent potential cheating on servers. The container class is now required in config for the anticheat to work  
Reworked ghost item handler, to make multiple config work on a same container GUI (such as different slot classes)  